<%= form_with(model: article_list) do |form| %>
  <% if article_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article_list.errors.count, "error") %> prohibited this article_list from being saved:</h2>

      <ul>
        <% article_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
